<div class="container">
	<h1>Sign In</h1>

	<%= form_for(:session, url: sessions_path) do |f| %>
		<%#= render "shared/error_messages" %>
		<%= f.label :email%>
		<%= f.text_field :email%>

		<%= f.label :password %>
		<%= f.password_field :password %>

		<%= f.submit "Sign In" %>
	<% end %>
</div>